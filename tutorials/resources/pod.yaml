apiVersion: v1
kind: Pod
metadata:
  name: nginx
  namespace: tutorial
spec:
  containers:
  - name: nginx
    image: cargo.caicloud.io/caicloud/nginx:1.9.3
    resources:
      requests:
        cpu: "0.1"
        memory: "100Mi"
      limits:
        cpu: "0.1"
        memory: "100Mi"
